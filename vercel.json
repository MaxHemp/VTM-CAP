{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "npx prisma generate && npx prisma migrate deploy && (if [ \"$SEED_ON_BUILD\" = \"1\" ]; then npx prisma db seed; fi) && npm run build"
}
